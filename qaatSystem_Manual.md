# نظام إدارة القات (qaatSystem) - دليل وتحليل شامل للنظام

نظام **qaatSystem** هو نظام محاسبي ومخزني متكامل مبني باستخدام إطار العمل **Laravel**، مصمم خصيصاً لإدارة عمليات بيع وتوزيع وتخزين نبات **القات** والعمليات المالية والمحاسبية المرتبطة بها.

يغطي النظام الجوانب الأساسية للدورة المستندية والمحاسبية من تهيئة الحسابات، وإدارة المخازن والأصناف، وصولاً إلى العمليات التشغيلية مثل المشتريات والمبيعات والعمليات النقدية والبنكية.

---

## 1. الهيكل العام للنظام والتقنيات المستخدمة

- **إطار العمل الأساسي:** Laravel (PHP)
- **قاعدة البيانات:** MySQL (عبر الهجرات Migrations والـ Eloquent Models)
- **واجهة المستخدم (Frontend):** Blade Templates مع استخدام لغة JavaScript متقدمة وتنسيقات مرنة تناسب الأنظمة المحاسبية باللغة العربية.
- **التصميم المعماري:** يتبع النمط المعماري MVC (Model-View-Controller) مع استخدام المراقبين (Observers) لتنفيذ منطق تلقائي عند تعديل البيانات (مثل مزامنة الحسابات مع الدليل المحاسبي).

---

## 2. المكونات الأساسية للنظام (System Modules)

ينقسم النظام وظيفياً إلى عدة موديولات متكاملة:

### أولاً: النظام المحاسبي والدليل المحاسبي (Accounting & Chart of Accounts)
يعتبر قلب النظام والمحرك الرئيسي للقيود والعمليات المالية:
- **دليل الحسابات (Chart of Accounts):** شجرة حسابات مرنة تدعم المستويات التجميعية والحسابات التحليلية الفرعية والقابلة للترحيل (`isPostable`).
- **إدارة العملات (Coins):** تهيئة العملات المختلفة وأسعار صرفها مقابل العملة المحلية ومتابعة حالتها نشطة/غير نشطة.
- **الصناديق النقدية (Boxes) والبنوك (Banks):** ربط الصناديق والبنوك مباشرة بحساباتها المقابلة في الدليل المحاسبي وتوليد كود تلقائي لكل حساب جديد وتتبع حالته.
- **مراقب الحسابات (Observers):** تتبع العمليات وتأمين سلامة البيانات المحاسبية عند الحذف والتحديث للعملاء، الموردين، الصناديق والبنوك لضمان توافقها مع الدليل المحاسبي.

### ثانياً: إدارة المخازن والأصناف (Inventory Management)
مصمم خصيصاً لطبيعة أصناف القات وتنوعها:
- **الأصناف (Items):** تعريف أصناف القات المختلفة وتحديد مواصفاتها وربطها بالوحدات والأنواع وتحديد حالتها (نشط / غير نشط).
- **الأنواع (Types):** تصنيف فرعي للأصناف (مثال: همداني، غيلي، أرحبّي، بلدي، إلخ).
- **الوحدات (Units):** تحديد وحدات القياس والوزن المستعملة (علاقة، حبة، كيس، إلخ).
- **المستودعات والمخازن (Warehouses / Stocks):** إدارة المخازن وتوزيع الأصناف عليها، مع توليد أكواد تسلسلية ذكية للمخازن الجدد.

### ثالثاً: إدارة المشتريات (Purchases Module)
يهتم بتسجيل فواتير الشراء وتوريد الأصناف للمخازن:
- **رأس الفاتورة (Purchase Invoice):** يحتوي على بيانات الفاتورة الأساسية مثل (رقم الفاتورة، التاريخ، حساب المورد، طريقة الدفع [نقدي، آجل، بنكي، شبكة]، العملة وسعر الصرف، المخزن المستقبل، المصاريف المصاحبة، الضرائب، كلفة النقل، الإجمالي العام).
- **تفاصيل الفاتورة (Invoice Details):** يحتوي على قائمة بالأصناف المشتراة مع تحديد الكميات والأسعار والخصم والنوع والوحدة لكل صنف.
- **العمليات والخصومات:** يقوم النظام بحساب إجمالي الفاتورة آلياً بعملة الفاتورة، مع توفير وصول ديناميكي للإجمالي المكافئ بالعملة المحلية بناءً على سعر الصرف الحالي.

### رابعاً: إدارة العملاء والموردين (Customers & Suppliers)
- **العملاء (Customers):** تسجيل بيانات العملاء، أرقام التواصل، وربطهم بحساباتهم الذممية في الدليل المحاسبي لمتابعة المديونيات.
- **الموردون (Suppliers):** تسجيل بيانات الموردين (المزارعين أو الموزعين الكبار للقات)، ومتابعة حساباتهم ومستحقاتهم المالية.

---

## 3. بنية قاعدة البيانات والجداول (Database Schema)

تحتوي قاعدة البيانات على جداول منظمة تمثل العلاقات الرابطة بين الكيانات:
1. **`characcount`:** يحتوي على دليل الحسابات (مع حقول: `accountID`, `accParent`, `accCode`, `accName`, `nature`, `accLevel`, `IsActive`, `isPostable`).
2. **`coins`:** بيانات العملات وأسعار الصرف.
3. **`boxes` / `banks`:** الصناديق والحسابات البنكية المرتبطة بدليل الحسابات.
4. **`items`:** الأصناف المخزنية مع حقل الحالة النشطة `is_active`.
5. **`type`:** أنواع القات وتصنيفاتها.
6. **`units`:** وحدات قياس الأصناف.
7. **`stocks`:** المخازن والمستودعات.
8. **`purchase_invoices`:** رأس فواتير الشراء.
9. **`purchase_invoice_details`:** تفاصيل الفواتير والكميات والأسعار.

---

## 4. المسارات (Routes) وهيكل التحكم

يحتوي ملف `routes/web.php` على مسارات منظمة ومقسمة بشكل احترافي لتغطية كافة شاشات وعمليات النظام، منها:
- **مسارات التهيئة والإعدادات المحاسبية:** `chartOfAccounts.*`, `coins.*`, `boxes.*`, `banks.*`
- **مسارات إعدادات المخازن:** `items.*`, `types.*`, `units.*`, `warehouses.*`
- **مسارات الأشخاص:** `customers.*`, `suppliers.*`
- **مسارات العمليات التشغيلية (Operations):**
  - فواتير الشراء: `invoicesPurch.*` (جلب رقم الفاتورة التالي، عرض الفاتورة، الحفظ، التعديل، والحذف).
  - سندات الصرف والقبض (Payment & Receipt Vouchers).
  - حركة المبيعات وحركات المخازن اليومية.

---

## 5. واجهة المستخدم والتصميم البصري (Blade Views)

تعتمد الواجهة على تقسيم منظم جداً للملفات في مجلد `resources/views`:
- **`layouts`:** تحتوي على الهيكل العام للنظام، وشريط التنقل العلوي `navbar` والقائمة الجانبية المنسدلة `sidebar`.
- **`dashboard`:** شاشات مؤشرات النظام، لوحة الإحصائيات المخزنية، ملخص العمليات، التنبيهات، وتوزيع الإمدادات.
- **نماذج البحث السريع والمودالات (Modals):** استخدام مكثف للمودالات المخصصة لإضافة وتحديث البيانات وحذفها بشكل فوري دون مغادرة الصفحة لضمان تجربة مستخدم سريعة وسلسة (مثل مودالات العملات، العملاء، الموردين، الأصناف، والوحدات).

---

### خلاصة
نظام **qaatSystem** يمثل حلًا برمجيًا متخصصًا ومتماسكًا، يمزج بين دقة المحاسبة المالية المتمثلة في شجرة الحسابات والدورة النقدية، وبين مرونة إدارة المخازن والمشتريات الخاصة بتجارة القات، معتمداً على واجهات مستخدم تفاعلية قوية تسهل من إدارة العمليات اليومية بكفاءة عالية.
